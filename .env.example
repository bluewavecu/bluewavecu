# Bluewave Credit Union environment variables
# Copy to .env for local development: cp .env.example .env

# PostgreSQL connection string (Render / Supabase / Vercel Postgres)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/bluewavecu

# JWT signing secret — use a long random string (32+ characters) in production
JWT_SECRET=replace-with-a-long-random-secret-at-least-32-characters

# Public app URL used for redirects, metadata, and cookie security context
# Local: http://localhost:3000
# Production: https://bluewavecu.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment (development | production | test)
NODE_ENV=development

# Allow demo seed in production (never enable on real production data)
ALLOW_DEMO_SEED=false

# Operations admin bootstrap (used by npm run bootstrap:admin on deploy)
# BOOTSTRAP_ADMIN_EMAIL=support@bluewavecu.com
# BOOTSTRAP_ADMIN_PASSWORD=
# Set BOOTSTRAP_ADMIN_RESET_PASSWORD=true to force-reset the operations password

# Resend transactional email (required in production for signup, login OTP, resets)
# Create an API key at https://resend.com/api-keys and verify bluewavecu.com in Resend Domains.
RESEND_API_KEY=
EMAIL_FROM="Bluewave Credit Union <support@bluewavecu.com>"
ADMIN_ALERT_EMAIL=support@bluewavecu.com

# Production cron bearer secret for POST /api/cron/run-jobs
# Render Cron Jobs: Authorization: Bearer <CRON_SECRET>
CRON_SECRET=
