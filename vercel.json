{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "export DATABASE_URL=\"${DATABASE_URL:-$POSTGRES_PRISMA_URL}\" && npx prisma generate && npx prisma migrate deploy && npm run build",
  "crons": [
    {
      "path": "/api/cron/run-jobs",
      "schedule": "0 * * * *"
    }
  ]
}
