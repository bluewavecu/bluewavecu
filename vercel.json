{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && export DATABASE_URL=\"${POSTGRES_URL_NON_POOLING:-${DATABASE_URL:-$POSTGRES_URL}}\" && npx prisma migrate deploy && npm run bootstrap:admin && npm run build",
  "crons": [
    {
      "path": "/api/cron/run-jobs",
      "schedule": "0 0 * * *"
    }
  ]
}
